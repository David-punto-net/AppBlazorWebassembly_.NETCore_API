@page "/ResendToken"
@if (loading)
{
    <Loading />
}

<div class="login-wrapper mt-5">
    <div class="login-container">

        <div class="col-md-12">
            <h3><i class="bi bi-envelope-at-fill"></i> Reenviar correo de confirmación de contraseña</h3>

            <EditForm Model="emailDTO" OnValidSubmit="ResendConfirmationEmailTokenAsync">
                <DataAnnotationsValidator />

                <div class="form-group">

                    <label>Email:</label>
                    <div>
                        <InputText class="form-control" @bind-Value="@emailDTO.Email" />
                        <ValidationMessage For="@(() => emailDTO.Email)" />
                    </div>
                </div>
    
                <div class="form-group mt-2">
                    <button class="btn btn-primary" type="submit"><i class="bi bi-send" /> Reenviar</button>
                </div>

            </EditForm>
        </div>

    </div>
</div>
